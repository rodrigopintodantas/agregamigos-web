# Prévia Open Graph (WhatsApp) para /{slug}/link-cadastro
#
# Opcao A (recomendada): build do front ja gera
#   dist/.../browser/{slug}/link-cadastro/index.html
# com meta og:* + app Angular. Basta publicar o build (npm run build).
# Defina PUBLIC_WEB_ORIGIN no build se o dominio nao for agregamigos.com.br:
#   PUBLIC_WEB_ORIGIN=https://seu-dominio.com.br npm run build
#
# Opcao B: nginx encaminha crawlers para a API (se nao usar paginas estaticas do build):
#
# map $http_user_agent $is_social_crawler {
#   default 0;
#   ~*(WhatsApp|facebookexternalhit|Facebot|Twitterbot|LinkedInBot|Slackbot|TelegramBot) 1;
# }
#
# location ~ ^/([a-z0-9-]+)/link-cadastro/?$ {
#   if ($is_social_crawler) {
#     rewrite ^ /og/link-cadastro/$1 break;
#     proxy_pass http://127.0.0.1:3000;
#     proxy_set_header Host $host;
#     proxy_set_header X-Forwarded-Host $host;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
#   try_files $uri $uri/ /index.html;
# }
